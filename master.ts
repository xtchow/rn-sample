import { StyleSheet } from "react-native";

export const master = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontSize: 28,
  },
  body: {
    fontSize: 16,
  },
  second: {
    fontSize: 14,
  },
});